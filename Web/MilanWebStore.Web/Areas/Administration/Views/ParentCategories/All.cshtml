@model AllParentChildCategoriesViewModel
@{
    this.ViewData["Title"] = "All Gender Categories";
    var counter = 1;
}

<div class="row">
    <div class="col-lg-12">
        <div class="card spur-card">
            <div class="card-header">
                <div class="spur-card-icon">
                    <i class="fas fa-table"></i>
                </div>
                <div class="spur-card-title">All Gender Categories</div>
            </div>
            <div class="card-body ">
                <table class="table table-hover table-in-card">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Name</th>
                            <th scope="col">Number of cloth categories</th>
                            <th scope="col">Edit</th>
                            <th scope="col">Delete</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var parent in Model.ParentCategories)
                        {
                            <tr>
                                <td>@(counter++)</td>
                                <td>@parent.Name</td>
                                <td>@parent.ChildCategoriesCount</td>
                                <td>
                                    <a asp-controller="ParentCategories" asp-action="Edit" asp-route-id="@parent.Id" class="btn btn-warning"><i class="fas fa-edit"></i></a>
                                </td>
                                <td>
                                    <a asp-controller="ParentCategories" asp-action="Delete" asp-route-id="@parent.Id" class="btn btn-danger"><i class="fas fa-trash-alt"></i></a>

                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


